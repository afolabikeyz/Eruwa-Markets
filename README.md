
  # Create Functional World-Class Design

  This is a code bundle for Create Functional World-Class Design. The original project is available at https://www.figma.com/design/zZqRCRmaRHljmod8wiiBjW/Create-Functional-World-Class-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  